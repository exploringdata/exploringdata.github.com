const hamburger=document.querySelector('.hamburger button');const navLinks=document.querySelector('.nav-links');hamburger.addEventListener('click',()=>{navLinks.style.display=navLinks.style.display==='flex'?'none':'flex'});function containerDim(selector,dim){const el=document.querySelector(selector);if(!el)return null;const value=getComputedStyle(el)[dim];return parseFloat(value)}document.addEventListener('click',e=>{const btn=e.target.closest('.close');if(!btn)return;const target=btn.closest('.close-target');if(target)target.style.display='none'});